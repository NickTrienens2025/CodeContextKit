# Project Mandates

- **Retrieval Strategy**: Always use `cckit` (CodeContextKit) commands (e.g., `cckit outline`, `cckit symbol`, `cckit map`) for reading file structure and symbol details instead of standard file reads when performing research or gathering context.
